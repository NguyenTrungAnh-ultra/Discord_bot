services:
  # --- MODULAR MONOLITH SERVICE ---
  discord-manager:
    build: .
    container_name: discord_manager
    restart: always
    env_file: .env
    # Mount source for data persistence (news history, snapshots)
    volumes:
      - ./temp:/app/temp
      - /etc/localtime:/etc/localtime:ro
    # Run the orchestrator
    command: python main.py
