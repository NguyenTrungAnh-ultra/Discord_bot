# Báo Cáo Chi Tiết - Report Collection System
**Ngày:** 19/01/2026  
**Module:** Report Collector & Webhook Sender

---

## 📋 TỔNG QUAN

Hệ thống thu thập và gửi báo cáo tài chính từ nhiều nguồn (VCBS, ACBS, KBSV, SSI, VietCap) đến Discord webhook mỗi sáng lúc 8:00 AM.

---

## ✅ CÔNG VIỆC ĐÃ HOÀN THÀNH

### 1. **Fix KBSV Scraper - Date Extraction**
**Vấn đề:** CSV cũ không có cột `date`, `report_id`, `ticker`  
**Giải pháp:**
- ✅ Updated `KBSV.py` để extract date từ `<span class="date">` element
- ✅ Format: `DD/MM/YYYY hh:mm:ss AM/PM` → `DD/MM/YYYY`
- ✅ Thêm `report_id` (MD5 hash), `ticker` extraction
- ✅ Re-scraped 500 báo cáo với structure mới
```python
# Location: src/modules/report_collecter/scanners/KBSV.py
# Lines: 119-156
- Extracts date from span.date element
- Parses DD/MM/YYYY format
- Adds to report dictionary
```

**Test Results:**
- ✅ 500 reports with complete columns
- ✅ Dates range: 07/01/2026 → older reports
- ✅ 29 reports in last 60 days

---

### 2. **Fix Date Comparison Bug in Webhook Sender**
**Vấn đề:** `self.today` (date object) vs `pd.Timestamp` → TypeError  
**Giải pháp:**
```python
# Location: src/modules/report_collecter/send_wehook.py
# Lines: 69-73
today_ts = pd.Timestamp(self.today)  # Convert to Timestamp
days_diff = (today_ts - report_date).days
return days_diff == 1  # Yesterday only
```

**Test Results:**
- ✅ Date filtering works correctly
- ✅ Found 79 reports in 60-day test range
- ✅ Filters yesterday's reports properly

---

### 3. **Fix Discord Timestamp Display**
**Vấn đề:** Hiển thị "Tomorrow at 12:43 AM" do dùng local timezone  
**Giải pháp:**
```python
# Location: src/modules/report_collecter/send_wehook.py
# Lines: 6, 202
from datetime import datetime, timezone

embed['timestamp'] = datetime.now(timezone.utc).isoformat()
```

**Test Results:**
- ✅ Timestamp hiển thị đúng trên Discord
- ✅ Uses UTC timezone

---

### 4. **PDF Download với Cookie Support**
**Vấn đề:** PDF không tải được, cần authentication  
**Giải pháp:**
```python
# Location: src/modules/report_collecter/send_wehook.py
# Lines: 119-172
- Load cookies từ ./temp/kbsv_cookies.pkl
- Use requests.Session() with cookies
- Download PDF trước khi gửi Discord
- Cache PDFs trong ./temp/reports/pdf_cache
```

**Test Results:**
- ✅ PDF downloaded successfully: 713KB (KBSV report)
- ✅ Cookies loaded from scraper sessions
- ✅ Gửi file đính kèm vào Discord webhook

---

### 5. **Fix NaN Handling**
**Vấn đề:** `ticker` và `pdf_url` có thể là NaN (pandas float) → TypeError  
**Giải pháp:**
```python
# Handle ticker NaN
ticker_raw = report.get('ticker', '')
ticker = str(ticker_raw).upper() if ticker_raw and not pd.isna(ticker_raw) else ''

# Handle PDF URL NaN
if not pdf_url or not isinstance(pdf_url, str) or 'http' not in pdf_url:
    return None
```

**Test Results:**
- ✅ No more TypeErrors
- ✅ Handles missing/invalid data gracefully

---

### 6. **Webhook Sender Safety Checks**
**Additions:**
```python
# Location: src/modules/report_collecter/send_wehook.py
# Lines: 91-94
# Skip sources without date column
if 'date' not in df.columns:
    print(f"  ⚠️ {source}: No date column, skipping")
    continue
```

**Test Results:**
- ✅ KBSV-Ngành skipped (no date column yet)
- ✅ System continues with other sources

---

### 7. **Main.py Integration**
**Updates:**
```python
# Location: main.py
# Lines: 13-16, 49-60, 68-75
- Added REPORT_SENDER_SCRIPT path
- Added job_send_reports() function
- Scheduled daily at 08:00
```

**Test Results:**
- ✅ Job configured in scheduler
- ⏳ Chưa test tự động (chưa đến 8AM ngày mai)

---

## 🐛 LỖI CÒN TỒN TẠI

### 1. **VCBS PDF URLs Missing**
**Mô tả:** VCBS reports không có `pdf_url` trực tiếp trong CSV (giá trị NaN)  
**Nguyên nhân:** VCBS website cần network interception để lấy PDF URL  
**Impact:** ⚠️ VCBS reports chỉ có link `detail_url`, không có PDF attachment  
**Workaround hiện tại:** Gửi link "📄 Xem PDF" thay vì file đính kèm  
**Cần fix:**
```python
# Option 1: Implement network interception in VCBS.py
# Option 2: Download PDF from detail_url page
# Option 3: Accept link-only for VCBS reports
```

---

### 2. **VietCap Scraper Chưa Test**
**Mô tả:** VietCap.py đã implement nhưng chưa chạy thử  
**Impact:** ⚠️ Không biết có hoạt động đúng không  
**Cần test:**
```bash
python -m src.modules.report_collecter.scanners.VietCap
```

---

### 3. **KBSV Other Categories Missing Dates**
**Mô tả:** Các category khác của KBSV (ngành, thị trường, chuyên đề) chưa có cột `date`  
**Files affected:**
- `./temp/reports/KBSV/bao_cao_nganh/kbsv_reports.csv` ❌
- `./temp/reports/KBSV/bao_cao_vimo/kbsv_reports.csv` ❌
- `./temp/reports/KBSV/bao_cao_chuyen_de/kbsv_reports.csv` ❌
- `./temp/reports/KBSV/toan_canh_thi_truong/kbsv_reports.csv` ❌

**Impact:** ⚠️ Các category này bị skip bởi webhook sender  
**Cần fix:**
```bash
# Delete old CSVs and re-run KBSV scraper for these categories
python -m src.modules.report_collecter.scanners.KBSV
# Or implement run() function cho từng category
```

---

## ⏳ CHƯA TEST

### 1. **Auto Schedule ở Main.py**
**Mô tả:** Job được schedule chạy lúc 8:00 AM mỗi ngày  
**Status:** ⏳ Chưa test tự động  
**Cần làm:** Chờ đến 8:00 AM ngày 20/01/2026 để verify

---

### 2. **ACBS, SSI Reports**
**Mô tả:** Scrapers đã implement nhưng chưa test webhook sending  
**Status:** ⏳ Chưa verify reports được gửi đúng  
**Cần test:**
```bash
# Run scrapers to get fresh data
python -m src.modules.report_collecter.scanners.ACBS
python -m src.modules.report_collecter.scanners.SSI

# Then test webhook sender
python -m src.modules.report_collecter.send_wehook
```

---

### 3. **Discord File Upload Limits**
**Mô tả:** Chưa test với file PDF lớn  
**Discord limits:**
- Free tier: 8MB per file
- Nitro: 50MB per file
**Status:** ⏳ Chưa test với PDFs > 8MB  
**Cần test:** Monitor logs cho file size warnings

---

### 4. **Error Recovery**
**Scenarios chưa test:**
- ❓ Webhook URL invalid/expired
- ❓ Network timeout during PDF download
- ❓ Discord rate limiting (too many messages)
- ❓ CSV file corrupted
- ❓ Disk space full (PDF cache)

---

## 📊 TEST RESULTS HÔM NAY

### Manual Tests Performed:

| Test | Status | Details |
|------|--------|---------|
| KBSV Date Extraction | ✅ | 500 reports, dates parsed correctly |
| Date Filtering (60 days) | ✅ | Found 79 reports |
| Date Filtering (yesterday) | ✅ | 0 reports (expected, no new reports yesterday) |  
| PDF Download (KBSV) | ✅ | 713KB, successful |
| Discord Webhook Send | ✅ | 1 report sent with PDF |
| Cookies Loading | ✅ | Loaded from kbsv_cookies.pkl |
| NaN Handling | ✅ | No TypeErrors |
| Timestamp UTC | ✅ | Displays correctly |

---

## 🔧 CẤU HÌNH PRODUCTION HIỆN TẠI

```python
# File: src/modules/report_collecter/send_wehook.py

# Date Filter
days_diff == 1  # Only yesterday's reports

# Limits
MAX_REPORTS = 20  # Max reports per run
PDF_CACHE_DIR = "./temp/reports/pdf_cache"
SENT_FILE = "./temp/reports/sent_reports.json"

# Sources
VCBS-DN        → ./temp/reports/VCBS/bao_cao_doanh_nghiep/vcbs_reports.csv (⚠️ No PDF URLs)
VCBS-Ngành     → ./temp/reports/VCBS/bao_cao_nganh/vcbs_reports.csv (⚠️ No PDF URLs)
ACBS-DN        → ./temp/reports/ACBS/bao_cao_doanh_nghiep/acbs_reports.csv (✅)
KBSV-DN        → ./temp/reports/KBSV/bao_cao_cong_ty/kbsv_reports.csv (✅)
KBSV-Ngành     → ./temp/reports/KBSV/bao_cao_nganh/kbsv_reports.csv (❌ No date column)
SSI            → ./temp/reports/SSI/ban_tin_thi_truong/ssi_reports.csv (⏳ Chưa test)

# Schedule (main.py)
Daily at 08:00 AM Vietnam time
```

---

## 📁 FILE STRUCTURE

```
Discord_bot/
├── main.py                          ✅ Updated with schedule
├── src/modules/report_collecter/
│   ├── send_wehook.py              ✅ Main webhook sender
│   └── scanners/
│       ├── VCBS.py                  ✅ Works (no PDF URLs)
│       ├── ACBS.py                  ✅ Works
│       ├── KBSV.py                  ✅ Fixed, re-scraped
│       ├── SSI.py                   ⏳ Chưa test webhook
│       └── VietCap.py               ❌ Chưa test
├── temp/
│   ├── kbsv_cookies.pkl            ✅ Used for PDF downloads
│   └── reports/
│       ├── sent_reports.json        ✅ Tracks sent reports
│       ├── pdf_cache/               ✅ Caches downloaded PDFs
│       ├── VCBS/                    ⚠️ No PDF URLs in CSV
│       ├── ACBS/                    ✅
│       ├── KBSV/
│       │   ├── bao_cao_cong_ty/    ✅ Has dates
│       │   ├── bao_cao_nganh/      ❌ No dates
│       │   ├── bao_cao_vimo/       ❌ No dates
│       │   └── bao_cao_chuyen_de/  ❌ No dates
│       └── SSI/                     ⏳ Chưa đầy đủ test
```

---

## 🎯 ROADMAP - CẦN LÀM TIẾP

### Priority 1: Critical
1. **Re-scrape KBSV other categories** với date extraction
   ```bash
   python -m src.modules.report_collecter.scanners.KBSV
   # Modify main to run all categories
   ```

2. **Fix VCBS PDF URLs**
   - Implement network interception hoặc
   - Download PDF từ detail_url page hoặc
   - Accept link-only cho VCBS

### Priority 2: Important  
3. **Test VietCap scraper**
   ```bash
   python -m src.modules.report_collecter.scanners.VietCap
   ```

4. **Test ACBS & SSI webhook sending**
   - Ensure reports are sent correctly
   - Verify PDF downloads

5. **Add error recovery**
   - Retry logic for failed downloads
   - Alert on critical failures
   - Disk space monitoring

### Priority 3: Nice to Have
6. **Improve logging**
   - Save logs to file
   - Add log rotation
   - Better error messages

7. **Performance optimization**
   - Parallel PDF downloads
   - Better caching strategy
   - Reduce redundant processing

8. **Monitoring dashboard**
   - Track success/failure rates
   - Monitor PDF cache size
   - Alert on anomalies

---

## 🔍 DEBUGGING NOTES

### Useful Commands:
```bash
# Test webhook sender (yesterday filter)
python -m src.modules.report_collecter.send_wehook

# Check CSV structure
python -c "import pandas as pd; df = pd.read_csv('./temp/reports/KBSV/bao_cao_cong_ty/kbsv_reports.csv'); print(df.columns.tolist())"

# Count reports by date
python -c "import pandas as pd; df = pd.read_csv('./temp/reports/KBSV/bao_cao_cong_ty/kbsv_reports.csv'); print(df['date'].value_counts().head(20))"

# Check sent reports
cat ./temp/reports/sent_reports.json | python -m json.tool

# Check PDF cache size
ls -lh ./temp/reports/pdf_cache | wc -l
```

### Common Issues:
```python
# Issue 1: NaN in ticker/pdf_url
# Solution: Use pd.isna() check before operations

# Issue 2: Date comparison TypeError  
# Solution: Convert to pd.Timestamp

# Issue 3: PDF download 403/401
# Solution: Load cookies from scraper sessions

# Issue 4: Unicode encoding errors
# Solution: Use 'utf-8-sig' encoding for CSVs
```

---

## 📝 NOTES

1. **Cookies Management:** Cookies được lưu trong `./temp/kbsv_cookies.pkl` sau mỗi lần scrape. Expire sau một thời gian → cần re-scrape để refresh.

2. **Sent Reports Tracking:** File `sent_reports.json` lưu report_ids đã gửi để tránh duplicate. Có thể safe delete file này nếu muốn gửi lại.

3. **PDF Cache:** PDFs được cache để tránh download trùng. Nên dọn dẹp định kỳ để tiết kiệm disk space.

4. **Discord Rate Limits:** 
   - 30 requests per minute per webhook
   - System sends with delay để tránh rate limit
   - MAX_REPORTS = 20 để an toàn

5. **Production Deployment:**
   - Đảm bảo `BAO_CAO_DOANH_NGHIEP` trong `.env`
   - Check disk space cho PDF cache
   - Monitor logs cho errors
   - Backup `sent_reports.json` định kỳ

---

## ✅ SYSTEM STATUS: READY FOR PRODUCTION*

_*With noted limitations for VCBS PDFs and KBSV other categories_

**Last Updated:** 19/01/2026 18:42  
**Next Test:** 20/01/2026 08:00 (Auto schedule)
